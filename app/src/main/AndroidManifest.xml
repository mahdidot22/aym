<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mahdi.d.o.taha.aym">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_mainicon"
        android:name=".Socket_Create"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_mainicon_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.AYM">
        <activity android:name=".Groups" />
        <activity android:name=".Msg" />
        <activity android:name=".Users" />
        <activity android:name=".athunpages.Signup" />
        <activity android:name=".athunpages.Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>